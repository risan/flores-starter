{% extends "layout.njk" %}

{% block content %}
  <h1>Latest Articles</h1>
  <ul>
    {% for post in collections.articles.slice(0, 5) %}
      <li>
        <a href="{{ post.url }}">
          {{ post.frontMatter.title }}
        </a>
      </li>
    {% endfor %}
  </ul>
{% endblock %}
