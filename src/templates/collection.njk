{% extends "layout.njk" %}

{% block content %}
  <h1>{{ frontMatter.title }}</h1>
  <ul>
    {% for post in collection %}
      <li>
        <a href="{{ post.url }}">
          {{ post.frontMatter.title }}
        </a>
      </li>
    {% endfor %}
  </ul>
{% endblock %}
